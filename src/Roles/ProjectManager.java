package Roles;
import Structure.*;

public class ProjectManager extends User{

    public ProjectManager(String name, String email, String password, Project project) {
        super(name, email, password);
    }

}
